{
  "name": "onsite",
  "version": "1.0.0",
  "description": "Save the days in which the worker is working on-site for the current month.",
  "main": "./bin/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "onsite": "./bin/index.js"
  },
  "author": "Gabriel Gusso",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.0.0",
    "wifi-name": "^3.1.1"
  }
}
